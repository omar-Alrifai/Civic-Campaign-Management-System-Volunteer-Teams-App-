import 'package:flutter/material.dart';

class AppColors {
  // --- Core Olive Tones (Retained & Refined) ---
  static const Color OliveGrove = Color(
    0xFF7D825F,
  ); // Slightly deeper, more grounded olive
  static const Color OliveMid = Color(
    0xFF8B937D,
  ); // Your CedarOlive, slightly renamed for clarity
  static const Color OliveLight = Color(
    0xFFADB5A4,
  ); // Your OliveMist, slightly more vibrant light olive

  // --- Prominent & Contrasting Accents ---
  static const Color SunsetOrange = Color(
    0xFFE57B5D,
  ); // A more vibrant, warm orange/coral
  static const Color OceanBlue = Color(0xFF5A80B7);
  static const Color MidGreen = Color(0xC5558C2E);
  static const Color DeepGreen = Color(
    0xC53C611E,
  ); // A clear, elegant blue for cool contrast
  static const Color RichBerry = Color(
    0xFF9E5C8D,
  ); // A unique, deeper berry/plum for strong accent

  // --- Neutrals (Essential for balance) ---
  static const Color CharcoalGrey = Color(
    0xFF333333,
  ); // A slightly softer dark grey than SmokyBasalt
  static const Color WhisperWhite = Color(
    0xFFF8F8F8,
  ); // A clean, almost pure white for backgrounds/text
  static const Color LightGrey = Color(
    0xFFE0E0E0,
  ); // A subtle light grey for dividers/borders
  static const Color CedarOlive = Color(0xFFA2A77F);
}
